# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/moj_portfel_2026

# JWT Configuration (generate with: openssl rand -base64 64)
JWT_SECRET=your_secret_key_here_minimum_64_characters_long
JWT_REFRESH_SECRET=your_refresh_secret_key_here_minimum_64_characters_long
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Server Configuration
NODE_ENV=development
PORT=4000

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5500

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Security
BCRYPT_ROUNDS=12
